// require the ORM here. will need the individual functionality that was made
// for each model.

// branch.selectAll({})

// branch.selectAndUpdate({})

// branch.deleteBranch({})

// branch.createBranch({})




// the CRUD functionality will be in the models files as well.
// then these files will be required in the Server.js file so that it has access.
